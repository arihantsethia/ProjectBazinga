<html>
  <head>
  </head>
  <body>
    <h2>WElcome to disscussion module </h2>
     <h3>Question # : {{ q_no }} </h3>
     <p > <b>Description : </b> {{ question.question_string }} </p>

     <ul class=entries > 
        {% for text in entries %}
          <li>
              user: {{ text.user_id }} -- {{ text.comment }}              
          </li>
        {% endfor %}
     </ul>  

     {% if session.logged_in %}
     <form action="{{ url_for('.add_question_comment') }}" method='GET'  >
      Comment: <input type='text' name='comment' />
      <input type='text' name='q_id' value={{ q_no }} style='display:none' />
      <input type='submit' value="comment"> 
    </form>
    {% endif %}
          
  </body>
</html>
